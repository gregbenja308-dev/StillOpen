[project]
name = "stillopen-jobs"
version = "0.1.0"
description = "Cloud Run Jobs: Watch / No-Show / retain."
requires-python = ">=3.12"
dependencies = [
    "stillopen-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stillopen_jobs"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
