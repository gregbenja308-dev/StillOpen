# 4-minute unedited beat

Record after a live Doc URL exists. Do not dump a personal window.

1. **Friction (20s)** — Open demo tabs. Say: “Chrome already groups these. That is not the job.”
2. **Twist (15s)** — “Still Open asks why the tab is still open, then files the work into Google before it closes anything.”
3. **File then close (90s)** — Ask “house shopping”. Hit **File, then close**. Show the **live Doc URL**. Tabs gone. Undo list shows exact titles. Original URLs stayed in session.
4. **Memory (30s)** — Memory tab: mutation `before → after`.
5. **Without you (40s)** — Cloud Console: Scheduler job → `POST /v1/jobs/watch`. Hash-only tick. No-Show or page-change Task.
6. **GCP (25s)** — Cloud Run `/healthz` + Firestore `plans` / `habits` documents.
7. **Bank stays off the model (20s)** — Chase tab still open. Surveyor / Clerk prompt: `blocked_from_model`. No extract.

Talking line if asked about tabs: “We read title and URL. Undo URLs never leave this laptop. Bank hosts never go to Gemini.”
